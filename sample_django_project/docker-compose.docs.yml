services:
  docs:
    image: sample_django_project_local_docs
    container_name: sample_django_project_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./sample_django_project:/app/sample_django_project:z
    ports:
      - '9000:9000'
    command: /start-docs
